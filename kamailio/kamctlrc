## kamctl/kamdbctl configuration for Docker environment

DBENGINE=PGSQL
DBRWUSER="${DB_USER:-kamailio}"
DBRWPW="${DB_PASS:-kamailio}"
DBROUSER="${DB_USER:-kamailio}"
DBROPW="${DB_PASS:-kamailio}"
DBHOST="127.0.0.1"
DBPORT="5432"
DBNAME="${DB_NAME:-kamailio}"

INSTALL_EXTRA_TABLES=yes
INSTALL_PRESENCE_TABLES=yes
INSTALL_DBUID_TABLES=yes
